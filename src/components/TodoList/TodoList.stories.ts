import type { Meta } from '@storybook/react';
import TodoList from './TodoList';

// Define the meta data for the component
const meta: Meta = {
  title: 'TodoList',
  component: TodoList
};

// Export the component and stories
export { TodoList };
export default meta;
